<Window x:Class="PM.InformationForm"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PM"
        mc:Ignorable="d"
        FontSize="13"
        FontFamily="Arial"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        WindowStartupLocation="CenterScreen"
        Title="InformationForm" Height="250" Width="300">
    <Grid Background="#008082">
        <Grid.RowDefinitions>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="1*"/>
        </Grid.RowDefinitions>
        <Border Grid.Column="0" Background="#FFF" BorderBrush="White" BorderThickness="1" CornerRadius="5" Margin="10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition/>
                    <RowDefinition/>
                </Grid.RowDefinitions>
                <Grid Grid.Row="0">
                    <Button Background="#008082" Opacity="0.8" BorderBrush="{x:Null}" HorizontalAlignment="Center" VerticalAlignment="Center" Width="150">
                        <Button.Content>
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Foreground="Yellow" VerticalAlignment="Center" Kind="Star"/>
                                <Label Content="WIP By MO"  Foreground="White"/>
                            </StackPanel>
                        </Button.Content>
                    </Button>
                </Grid>
                <Grid Grid.Row="1">
                    <Button Background="#008082" Opacity="0.8" HorizontalAlignment="Center" VerticalAlignment="Center" Width="150" BorderBrush="{x:Null}">
                        <Button.Content>
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Foreground="Yellow" VerticalAlignment="Center" Kind="Star"/>
                                <Label Content="WIP By Model"  Foreground="White"/>
                            </StackPanel>
                        </Button.Content>
                    </Button>
                </Grid>
            </Grid>
        </Border>
        <Border Grid.Row="1" BorderBrush="White" BorderThickness="1" CornerRadius="5" Background="White" Margin="10">
            <Button Background="#008082" Opacity="0.8" BorderBrush="{x:Null}" HorizontalAlignment="Center" VerticalAlignment="Center" Width="90">
                <Button.Content>
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Foreground="Yellow" VerticalAlignment="Center" Kind="Check"/>
                        <Label Content="OK" Foreground="White"/>
                    </StackPanel>
                </Button.Content>
            </Button>
        </Border>
    </Grid>
</Window>
